world wide web - www
tcp/ip

HTML - HYPERTEXT MARKUP LANGUAGE - TEXT FORMAT 
HTTP - HYPERTEXT TRANSFER PROTOCOL - PROTOCOL
CLIENT - BROWSER/PHONE/ETC
SERVER - GIVES INFORMATION-MESSAGE

          REQUEST (URL)
	   ------------->
CLIENT                 SERVER
	   <-------------
	   	  RESPONSE (PAGE/DATA - HTML)
	   	  
	   	  
WEB APPLICATION
APPLICATION RUNNING OVER THE WEB, IT CAN BE A WEBSITE, DYNAMIC DATA AND PAGES - ENTREPRISE APPLICATION

HTTP (HTTP PROTOCOL) - TRANSFER IN OUR WEB APPLICATION
WEB SERVER - TOMCAT/WILDFLY

HTTP REQUEST
METHODS: 
	GET - 
	POST -
	DELETE
	PUT
	UPDATE
	PATCH
	
SERVLET
JAVA PROGRAMS RUNNING INSIDE JVM THAT RESPONDS TO HTTP REQUEST.

1. READ DATA
2. PROCESS DATA
3. RESPONDS TO THE REQUEST

SERVLET CONTAINER - WEB SERVER- TOMCAT-WILDFLY

SERVLET API - WE NEED SERVLET APIs to create servlet

Wildfly
change directory to wildfly-version/bin
unix/linux - sh standalone.sh or ./standalone.sh
windows - standalone.bat

INTERFACES
a) HttpServletRequest
1. getParameter(String name);
2. getParameterNames()
3. getParameterValues
4. getHeader()
5. getHeaders()
6. getHeaderNames()

b) HttpServletResponse

c) RequestDispatcher - forwarding & including

d) ServletConfig - init params

e) ServletContext -

f) HttpSession - session management

g) Filters

i) Listeners & Events

h) Servlet - write html inside java

j) Jsp -  write java inside html
    index.jsp -> Index_jsp.java -> Index_jsp.class

    Scripting Element
    a) Comment <%-- comment --%>
    b) Directive - they direction to server regarding the page
               - <%@ directive %>
               example
               <%@ page import="" %>
    c) Declaration <%! declarations %>
                   <%! int loginTime = new Date() %>

    d) Scriptlets <% code %>

    e) Expression - display as string
            <%= loginTime %>

    Implicit Objects
    a) out = JspWriter
        - out.print();
        - out.println();
        - out.newLine();
    b) request = HttpServletRequest
        - request.getParameter(String name);
        - request.getParameterNames()
        - request.getParameterValues()
        - request.getParameterMap()
        - request.setAttribute()
        - request.getHeader(String name)
    c) response = HttpServletResponse
        - response.setContentType(String type)
        - response.setHeader(String name, String value)
        - response.getWriter()
    d) session = HttpSession
    e) application = ServletContext
    f) config = ServletConfig

    JSP ACTIONS
        standard tags that simplify code.
        <jsp:text>
        <jsp:body>
        <jsp:include> - include another resource to the current JSP page
        <jsp:forward> - forwarding request to the another resource
        <jsp:param>

        <jsp:useBean>
        <jsp:setProperty>
        <jsp:getProperty>

    JSP EXPRESSIONS LANGUAGE (EL)
        -to simplify data access of java beans and implicit objects
        -arithmetic +,-,*, /, %
            ${20/10} or ${beanName.number/beanName.number2}
        -Logical and relation operators
            == and eq
            != and ne
            < and lt
            > and gt
            >= and ge
            <= and le

    JSTL - JSP Standard Tag Library
        collection of ready-made tags..that
        replaces scriptlets, makes jsp cleaner, readable
        Library
            1. Core - Logic, Loops, Variables -- prefix c *
            2. Formatting - format dates, numbers etc fmt *
            3. Functions - prebuilt function..methods fn *


            4. SQL prefix - sql
            5. XML prefix - x

        Core Library
            <c:out> - output data
            <c:set> - set variable
            <c:if>
            <c:choose>
            <c:forEach>


Convert Db session to RDBMS (MYSQL, POSTGRES..ORACLE)
Make jdbc to be single...use ServletContextListener..
on startup
    1. System should create database automatically if it does not exist
    2. System should create table of models/entities automatically if the dont exist
    3. Queries should also be generated dynamically

CONTEXT DEPENDENCY INJECTION(CDI)
    IoC (principle) - giving a container/framework object creation
        and management
        - no longer do object creation and management in code

    Dependency Injection (DI) - implementation of IOC

    in JEE Dependency Injection is achieved through
        Context Dependency Injection (CDI)
        - JEE specification for DI
        -type-safe,
        flexible and manage dependencies of object lifecycle

    KEY CONCEPTS
        a) context - environment/scope that the object exists
        b) dependency injection - object created automatically
        c) CDI containers - the engine that creates
            and manages the objects server/framework
            - in my case wildfly
        d) bean - java class(blueprint) to be management by containers
        e) contextual instance - which an object created by
            the container from bean.

    CDI CONTEXTS(SCOPE)
    @ApplicationScope - one object/instance for the entire app
    @RequestScope - new object/instance per request
    @SessionScope - one object/instance per session
    @Dependent - new object/instance per injection (@Inject)


    QUALIFIERS
        - Injection Points
            -Fields
            -Constructor ---- assignment
            -Setter Method --- assignment
        -Multiple qualifiers

        -Built in qualifiers
            -@Named
                -Constructor ---- assignment.
                -Setter Method --- assignment.
            -@Default
            -@Any
            -@Alternatives -- assignment.

        -also inject datasource ---------assignment

    PRODUCER
        @Produces tells CDI container...that whenever
        someone injects this class, use this method/logic
        to create the instance.

        CDI Container normally create instance on its
        own way on injections...producers use my provide
        method to create that instance and not your own
        way.

        Ways of using producer
            -customize how contextual instance is created.
            -use for object that cannot
                directly be instantiated int, String,
                CalculateAge

POJO - Plain Old Java Object
JavaBeans
    - No arg constructors
    - private properties with mutators and accessors
    - Serializable - save object, transfer

JEE -
    EJB - Enterprise Java Beans

    KEY CONCEPTS
    TYPES OF BEANS
        -Session Bean
            -Stateless - the client does not keep track the
                state of the instance...EJB can be inject
                with annotation @EJB..that object/class
                that injects that EJB is called client..
                a client another EJB, Servlet and a
                managed Bean...
            -Stateful
            -Singleton
        -Message Driven




JPA & JTA (INCLUDE BEAN VALIDATION)
WEB SERVICES
    -JAX-RS - REST
    -JAX-WS - SOAP
    -WEBSOCKET
JMS
JAKARTA SECURITY

PROJECT STRUCTURE

ACTION(url/web servlet)    REST-Endpoints   Soap-Endpoints
        |                       |               |
        |                       |               |
---------------------------------------------------------
                        |
                        |
  Business Logic(EJBs - CDIs) - no db connection(access data through DAO)
                        |
                        |
                 Data Access Objects (DAO) EM(Entity Manager)
                        |
                        |
                   Model (Entity) JPA
                        |
                        | ORM (Object relation mapping)
                        |
                     Database


